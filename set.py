# set = collection which is unordered, unindexed, no duplicate value
# set is faster mor than a list if you want to find a vlue is exist or not

tools = {"hummer", "wrench", "pin","Benz"}
cars = {"Benz", "BMW", "TOYOTA"}

tools.add("meter")
# tools.remove("hummer")
# tools.clear()
#tools.update(cars)
#newSet = tools.union(cars)

#print(tools.difference(cars))
#print(tools.intersection(cars))
#for x in newSet:
# print(x)
